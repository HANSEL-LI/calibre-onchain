"""calibre_agent — standalone on-chain market-maker agent.

W0 SCAFFOLD — placeholder only. The agent reads calibre's public price feed as
its prior (over HTTP, Seam 4) and quotes on the Arc CalibreMarket contract from
its own Dynamic server wallet. Newly created during the hackathon; it does not
import or mirror the private bot fleet. Implementation lands in W7.2.
"""

__version__ = "0.0.0"
