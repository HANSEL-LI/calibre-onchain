/**
 * Discord role-sync bot.
 *
 * W0 SCAFFOLD — placeholder entrypoint only. The bot will resolve guild
 * members' `*.calibre.eth` subnames, read the `gg.calibre.rank` text record
 * (via the ENS gateway / public profile API, Seam 2), and assign the matching
 * Discord role. It never touches the database. Implementation lands in W6.4.
 */
export function main(): void {
  throw new Error("calibre-onchain discord-bot: not implemented (W0 scaffold; see W6.4)");
}
